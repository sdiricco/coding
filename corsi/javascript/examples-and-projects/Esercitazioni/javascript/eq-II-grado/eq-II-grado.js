// window object
//Il DOM fa parte del window object

// In node js si creano applicazioni desktop
// Il front end sono tutte le richieste mentre il backend corrisponde proprio
// al proprio computer, quindi funziona da server, cioè quello che esegue le richieste
// In una programmazione front end invece tutte le richieste vengono eseguite da un altro
// computer (server) e il browser funge da interfaccia di comunicazione richieste / servizi


//window è un oggetto che rappresenta il TUTTO. Qui sono definite tutte le variabili gloabali, metodi definiti da JS
console.log(window);
